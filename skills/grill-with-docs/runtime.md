Sharpen the user's plan or design while composing the upstream grilling and
domain-modeling skills lazily.

Separate unknowns into facts to investigate and decisions for the user. Investigate
facts yourself through available connected capabilities; ask the user only genuine
decisions.

When the first genuine decision frontier is ready, call
`load_skill("grilling")` and follow the loaded skill. Do not preload it.

When active domain-modeling work is needed because terminology is being sharpened or
resolved, or a consequential decision may deserve durable documentation, call
`load_skill("domain-modeling")` and follow the loaded skill. Do not preload it.

If repository writes are unavailable, state explicitly that the resulting domain
documentation was not persisted. Do not produce a specification or begin
implementation as part of this workflow.
