{
  "name": "codebase-design",
  "visibility": "hidden",
  "description": "Provide canonical deep-module vocabulary and principles for architecture analysis.",
  "dependencies": [],
  "upstream": {
    "repository": "https://github.com/mattpocock/skills",
    "location": "skills/engineering/codebase-design/SKILL.md",
    "commit": "6654f6b60cd9d5be8b54c6fafe44346dabeb3b76"
  },
  "license": "MIT",
  "attribution": "Copyright (c) 2026 Matt Pocock",
  "projection": {
    "entrypoint": "upstream.md",
    "sources": [
      {
        "path": "upstream.md",
        "sha256": "2c20617f87ec8af6a434859f381b2f061a69b530444e74eb39e78bb016a6d1e2"
      },
      {
        "path": "upstream-deepening.md",
        "sha256": "f3dd099ce99289bd213914d8ee3e2429b78309c3957ca4583f7659551b1d53c1"
      },
      {
        "path": "upstream-design-it-twice.md",
        "sha256": "8e740bf98446dbd4dfdc132ac4346d9a7eedaf93de6a495889171cf7f99f16bd"
      }
    ],
    "changeRecords": [
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream.md",
        "evidence": "The exact pinned DEEPENING.md Supporting Document is appended to the loaded runtime. Retargeting the relative link to that inlined document preserves the upstream deepening methodology without requiring a local filesystem.",
        "transform": {
          "type": "replace-exact",
          "match": "- **Deepening a cluster given its dependencies**, see [DEEPENING.md](DEEPENING.md): dependency categories, seam discipline, and replace-don't-layer testing.",
          "replacement": "- **Deepening a cluster given its dependencies**, see the exact pinned **Deepening** Supporting Document appended below: dependency categories, seam discipline, and replace-don't-layer testing."
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream.md",
        "evidence": "The exact pinned DESIGN-IT-TWICE.md Supporting Document is appended below. The Target Runtime can use that parallel pattern only when independent children have direct connected GitHub access; otherwise pretending parallel independence would violate the upstream comparison method.",
        "transform": {
          "type": "replace-exact",
          "match": "- **Exploring alternative interfaces**, see [DESIGN-IT-TWICE.md](DESIGN-IT-TWICE.md): spin up parallel sub-agents to design the interface several radically different ways, then compare on depth, locality, and seam placement.",
          "replacement": "- **Exploring alternative interfaces**, see the exact pinned **Design It Twice** Supporting Document appended below: use parallel independent child explorations only when each child has direct connected GitHub access and remains isolated until comparison; otherwise stop that alternative-interface branch rather than pretending parallel independence."
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-deepening.md",
        "evidence": "The Supporting Document is appended into the same Generated Runtime as its SKILL.md entrypoint, so the local relative SKILL.md link is retargeted to the entrypoint above without changing the referenced vocabulary.",
        "transform": {
          "type": "replace-exact",
          "match": "How to deepen a cluster of shallow modules safely, given its dependencies. Assumes the vocabulary in [SKILL.md](SKILL.md): **module**, **interface**, **seam**, **adapter**.",
          "replacement": "How to deepen a cluster of shallow modules safely, given its dependencies. Assumes the vocabulary in the Codebase Design entrypoint above: **module**, **interface**, **seam**, **adapter**."
        }
      },
      {
        "allowedRuntimeChange": "inline-supporting-document",
        "source": "upstream-deepening.md",
        "evidence": "DEEPENING.md is required upstream methodology. Append the exact pinned document, after only its recorded self-containment link translation, so the loaded runtime preserves dependency categories, seam discipline, and replace-don't-layer testing.",
        "transform": {
          "type": "append-source",
          "separator": "\n\n---\n\n"
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The Supporting Document is appended into the same Generated Runtime as its SKILL.md entrypoint, so the local relative SKILL.md link is retargeted to the entrypoint above.",
        "transform": {
          "type": "replace-exact",
          "match": "Uses the vocabulary in [SKILL.md](SKILL.md): **module**, **interface**, **seam**, **adapter**, **leverage**.",
          "replacement": "Uses the vocabulary in the Codebase Design entrypoint above: **module**, **interface**, **seam**, **adapter**, **leverage**."
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The exact pinned DEEPENING.md document is already appended into this runtime, so this relative reference is retargeted to that inlined methodology.",
        "transform": {
          "type": "replace-exact",
          "match": "- The dependencies it would rely on, and which category they fall into (see [DEEPENING.md](DEEPENING.md))",
          "replacement": "- The dependencies it would rely on, and which category they fall into (see the inlined **Deepening** Supporting Document above)"
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The Target Runtime may not expose independently isolated child conversations with direct repository access. Preserve the upstream parallel-design method only when that equivalent capability is live; otherwise stop the branch rather than simulating independence.",
        "transform": {
          "type": "replace-exact",
          "match": "Spawn 3+ sub-agents in parallel. Each must produce a **radically different** interface for the deepened module.",
          "replacement": "Spawn 3+ independent child explorations in parallel only when each child can access the repository directly through connected GitHub capabilities and their findings remain isolated until comparison. Each must produce a **radically different** interface for the deepened module. If that independent child mechanism or direct repository access is unavailable, stop this alternative-interface branch rather than simulating parallel independence in one context."
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The exact pinned Deepening Supporting Document is inlined into the Generated Runtime, so this technical-brief reference is retargeted to the same methodology.",
        "transform": {
          "type": "replace-exact",
          "match": "Prompt each sub-agent with a separate technical brief (file paths, coupling details, dependency category from [DEEPENING.md](DEEPENING.md), what sits behind the seam).",
          "replacement": "Prompt each sub-agent with a separate technical brief (file paths, coupling details, dependency category from the inlined **Deepening** Supporting Document above, what sits behind the seam)."
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The exact pinned Deepening Supporting Document is inlined into the Generated Runtime, so this output-contract reference is retargeted to the same methodology.",
        "transform": {
          "type": "replace-exact",
          "match": "4. Dependency strategy and adapters (see [DEEPENING.md](DEEPENING.md))",
          "replacement": "4. Dependency strategy and adapters (see the inlined **Deepening** Supporting Document above)"
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream-design-it-twice.md",
        "evidence": "The Design It Twice Supporting Document is inlined into the same Generated Runtime as the Codebase Design entrypoint, so this remaining local SKILL.md vocabulary reference is retargeted to the entrypoint above without changing the required vocabulary.",
        "transform": {
          "type": "replace-exact",
          "match": "Include both [SKILL.md](SKILL.md) vocabulary and CONTEXT.md vocabulary in the brief so each sub-agent names things consistently with the architecture language and the project's domain language.",
          "replacement": "Include both the Codebase Design entrypoint vocabulary above and CONTEXT.md vocabulary in the brief so each sub-agent names things consistently with the architecture language and the project's domain language."
        }
      },
      {
        "allowedRuntimeChange": "inline-supporting-document",
        "source": "upstream-design-it-twice.md",
        "evidence": "DESIGN-IT-TWICE.md is required upstream methodology. Append the pinned document after only the recorded self-containment and capability translations.",
        "transform": {
          "type": "append-source",
          "separator": "\n\n---\n\n"
        }
      }
    ]
  }
}
