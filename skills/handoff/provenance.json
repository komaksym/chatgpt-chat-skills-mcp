{
  "name": "handoff",
  "visibility": "public",
  "description": "Create a compact continuation brief for another conversation.",
  "dependencies": [],
  "sourceProvenance": {
    "type": "pinned-github",
    "repository": "https://github.com/mattpocock/skills",
    "commit": "6654f6b60cd9d5be8b54c6fafe44346dabeb3b76",
    "license": "MIT",
    "attribution": "Copyright (c) 2026 Matt Pocock"
  },
  "projection": {
    "entrypoint": "upstream.md",
    "sources": [
      {
        "path": "upstream.md",
        "sha256": "7c62de979fdc7ac32fb5ddb2146156c917f80ee070d30fadc9d40343c4b6ed25",
        "upstreamPath": "skills/productivity/handoff/SKILL.md"
      }
    ],
    "changeRecords": [
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "chrome-browser-mcp"
          ],
          "incompatibility": "The generated runtime now transfers a handoff prompt into a fresh ChatGPT session through Chrome Browser MCP, so its public metadata must describe the prompt artifact that the runtime actually constructs."
        },
        "transform": {
          "type": "replace-exact",
          "match": "---\nname: handoff\ndescription: Compact the current conversation into a handoff document for another agent to pick up.\nargument-hint: \"What will the next session be used for?\"\ndisable-model-invocation: true\n---",
          "replacement": "---\nname: handoff\ndescription: Compact the current conversation into a handoff prompt for another agent to pick up.\nargument-hint: What will the next session be used for?\ndisable-model-invocation: true\n---"
        }
      },
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "upstream.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "chrome-browser-mcp",
            "no-arbitrary-host-filesystem"
          ],
          "incompatibility": "The Target Runtime Profile cannot rely on an OS temporary directory but can transfer the handoff directly through connected Chrome Browser MCP. The browser capability must still be observed live, and failure must leave the old agent tab intact."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Write a handoff document summarising the current conversation so a fresh agent can continue the work. Save to the temporary directory of the user's OS - not the current workspace.",
          "replacement": "Write a compact handoff prompt summarising the current conversation so a fresh agent can continue the work. Include only context that is still needed for continuation. Return the handoff directly in chat as a single fenced Markdown block so the user retains a one-click copy fallback. Do not create a separate document, artifact, or file.\n\nAfter constructing the handoff prompt, append exactly one final line:\n`@skills-mcp tool implement()`\nDo not add anything after that line.\n\nThen transfer the entire handoff prompt through the connected Chrome Browser MCP. Call `get_active_tab()` before opening anything and retain that ChatGPT tab ID as the old agent tab. Call `new_tab()` with `https://chatgpt.com/` and `active: false` so the fresh ChatGPT conversation opens in the background without stealing focus. Call `read_tab()` on the new tab to identify its message composer, then call `type()` to place the full handoff prompt into that composer. Call `read_tab()` again and verify that the composer contains the transferred handoff prompt including the exact final `@skills-mcp tool implement()` line. Treat the transfer as successful only after this verification succeeds. Only then call `close_tab()` with the old agent tab ID.\n\nIf Chrome Browser MCP is unavailable, the old agent tab cannot be identified, the background tab cannot be opened, the prompt cannot be placed, or verification fails, do not call `close_tab()`. Report the transfer as incomplete, leave the old agent tab open, and do not claim the handoff is complete."
        }
      },
      {
        "allowedRuntimeChange": "translate-invocation-or-tool",
        "source": "upstream.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "only-load-skill-and-list-skills"
          ],
          "incompatibility": "The Target Runtime Profile invokes skills through the Skills MCP load_skill tool and does not expose the upstream Skill tool."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Include a \"suggested skills\" section in the document, naming which skills the next agent should call the Skill tool for.",
          "replacement": "Include a \"suggested skills\" section in the prompt, naming which skills the next agent should load through the Skills MCP with `load_skill`."
        }
      }
    ]
  }
}
