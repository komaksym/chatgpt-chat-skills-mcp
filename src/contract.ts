export const REMOTE_EXECUTION_CONTRACT = `# Remote execution contract

Work against the active remote GitHub repository through connected tools. Do not
assume a local checkout, shell, filesystem, Git CLI, background execution, or write
access. Use other connected capabilities when available. Claim an action or
verification only after observing its result. If a required capability is absent,
state exactly what could not be completed.`;
